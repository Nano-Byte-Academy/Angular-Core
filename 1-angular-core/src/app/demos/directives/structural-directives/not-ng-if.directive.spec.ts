import { NotNgIfDirective } from './not-ng-if.directive';

describe('NotNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NotNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
